-- TASK 10: List all records from 'second_table' sorted by score (descending)

-- This query selects 'score' and 'name' columns and orders by score from highest to lowest

SELECT score, name FROM second_table
ORDER BY score DESC;