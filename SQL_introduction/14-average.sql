-- TASK 14: Compute the average score from all rows in 'second_table'

-- This query calculates the average value of the column 'score'
-- The result column must be named 'average'

SELECT AVG(score) AS average FROM second_table;