#!/usr/bin/python3
"""
Unittests for max_integer() function.
Covers common cases, edge cases, and empty input.
Follows Python's unittest module conventions.
"""

# Étape 1 : importer le module unittest
# Étape 2 : importer la fonction max_integer depuis le bon module (à adapter si elle est dans un autre fichier)

# Étape 3 : créer une classe de test qui hérite de unittest.TestCase

    # Étape 4 : définir une méthode pour tester une liste vide
        # ➤ utiliser self.assertEqual(...)

    # Étape 5 : définir une méthode pour tester une liste avec un seul élément

    # Étape 6 : définir une méthode pour tester une liste de plusieurs entiers (valeur max en fin)

    # Étape 7 : définir une méthode pour tester une liste de plusieurs entiers (valeur max au début)

    # Étape 8 : définir une méthode pour tester une liste de plusieurs entiers (valeur max au milieu)

    # Étape 9 : définir une méthode pour tester une liste avec plusieurs fois la même valeur max

    # Étape 10 : définir une méthode pour tester une liste avec des nombres négatifs

    # Étape 11 : définir une méthode pour tester une liste avec des floats (optionnel)

    # Étape 12 : définir une méthode pour tester l’appel sans argument (valeur par défaut)

# Étape 13 : ajouter le bloc if __name__ == "__main__" pour lancer les tests si fichier exécuté directement
